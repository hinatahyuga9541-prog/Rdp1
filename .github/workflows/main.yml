name: RDP - 128GB Paid Runner

on:
  workflow_dispatch:

jobs:
  rdp-128gb:
    runs-on: windows-128gb   # 👈 PAID 128 GB runner label
    timeout-minutes: 720     # max allowed by your plan

    steps:
      - name: Check System Specs
        run: |
          systeminfo | findstr /C:"Total Physical Memory"
          wmic cpu get NumberOfCores,NumberOfLogicalProcessors

      - name: Enable RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
            -Name "fDenyTSConnections" -Value 0 -Force

          netsh advfirewall firewall add rule name="RDP" `
            dir=in action=allow protocol=TCP localport=3389

          Restart-Service TermService -Force

      - name: Create RDP User
        run: |
          $password = "RDP@128$(Get-Random -Maximum 9999)"
          $secure = ConvertTo-SecureString $password -AsPlainText -Force

          New-LocalUser -Name "RDP" -Password $secure -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

          echo "RDP_USER=RDP" >> $env:GITHUB_ENV
          echo "RDP_PASS=$password" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          Invoke-WebRequest https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi `
            -OutFile ts.msi
          Start-Process msiexec.exe -ArgumentList "/i ts.msi /quiet /norestart" -Wait

      - name: Connect Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=gh-128gb-${{ github.run_id }}

          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          echo "TS_IP=$ip" >> $env:GITHUB_ENV

      - name: RDP Details
        run: |
          Write-Host "=============================="
          Write-Host "RDP IP : $env:TS_IP"
          Write-Host "User   : $env:RDP_USER"
          Write-Host "Pass   : $env:RDP_PASS"
          Write-Host "RAM    : 128 GB (Paid Runner)"
          Write-Host "=============================="

      - name: Keep Session Alive
        run: |
          while ($true) {
            Write-Host "RDP running on 128GB RAM - $(Get-Date)"
            Start-Sleep 300
          }
